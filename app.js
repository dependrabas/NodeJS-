// const names = require('./4-names');
// const { sayHello } = require('./5-utils');  // Destructuring the imported module

// const myname = require('./alternative');

// require('./mind');
// console.log(myname);

// sayHello(names.depen);
// sayHello(names.depend);


const os = require(os);

const user = os.userInfo();
console.log(user);